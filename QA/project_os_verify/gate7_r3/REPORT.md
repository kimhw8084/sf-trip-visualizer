# Gate 7 / CHG-67 R3 provenance recovery

Project: `sf-trip-visualizer`  
Mode: evidence-only verification  
Classification: `GO_READY_EVIDENCE`

This report is evidence for Project OS to audit. It does not claim Gate 7 GO, integration, Gate 5 completion, live release, or overall production readiness.

## Authority and immutability

The fresh transport base was `bb1b58619629448d99cd6a20b27e882492d1d0d7` with tree `f6a8c4333fd37c49d1a0ca09e604a87005c9f7eb`. The immutable product/source candidate was `fed7f8394b2d8edfe7a6ff933dbf8b49bdb28aa3` with tree `21cb22d44960eb14b326d8e671c6db5bc552ea25`. The historical build commit was `3455a3ec339f434d840551f425ee7b68a690377f` with tree `0f68fae6835340083dbe9f0cb4af7383c12f750b`.

Both commits were inspected in clean detached worktrees outside the repository. The candidate and historical `vendor/trip-vector.js` are identical: 73,896 bytes, 920 lines, SHA-256 `fccb368f5ec3662547599238ca893761fa17f52ca25eb1875ad89db1ef51554d`.

R2 final `7e3f7918033ccddc92eef9449a11096bfedfd408` is an ancestor of this verification branch’s source context but is not product-source authority. The candidate-to-R2 diff contains only these QA/evidence paths:

- `QA/map_first/canonical_truth.json`
- `QA/map_first/cross_browser.json`
- `QA/map_first/full_acceptance.json`
- `QA/project_os_verify/gate7_r2/evidence.json`
- `QA/route_panel/route_explanations_panel.json`

No product/runtime/workflow/dependency/manifest/vendor path changed in that tail.

## Exact bundle recovery

The historical tracked entrypoint is:

```js
import { Protocol, PMTiles, FileSource } from 'pmtiles';
import { layers, namedFlavor } from '@protomaps/basemaps';

window.TRIP_VECTOR = { Protocol, PMTiles, FileSource, layers, namedFlavor };
```

The bundle’s module comments identify `fflate`, `pmtiles`, `@protomaps/basemaps`, and the entrypoint. No JS package manifest, lockfile, dependency tree, or bundler configuration was tracked by the historical commit.

The registry search cutoff was the historical commit time, `2026-09-15T14:31:02Z`. Stable releases after the cutoff and prereleases were excluded. The eligible search space was 58 `fflate`, 65 `pmtiles`, 15 `@protomaps/basemaps`, and 482 `esbuild` releases. The exact tested tuple was:

| Component | Version | Registry identity | Source identity |
|---|---:|---|---|
| `fflate` | `0.8.3` | npm integrity `sha512-tbZNuJrLwGUp3zshBtdy4W+ORxZuIh8a5ilyIEQDC5rY1f3U20JMry0Ll3WBzU58EZKsEuJFXhb5gwv8CsPvgA==`; tarball SHA-256 `38c2cd824402407b43153c782274aec2ea83ea688e4aa0b743c5f2c305857d92` | tag `v0.8.3`, commit `a661bab4c135c368b6e68af3be1904f89ed070cf` |
| `pmtiles` | `4.5.0` | npm integrity `sha512-CBeD4SoUluFziGdy/8k7FOjQxQy486n+929W/tWophauvMICpkZ26vGBWhDt/6b1FZQWNaf4jFdT/5+q0Wii5w==`; tarball SHA-256 `23ae7c575578ad24cd579377d69c46550631da219e6f179997ec2cf3b8c937e5` | gitHead `3b10e67edb65c6b04549f74c0279cef8328d859c` |
| `@protomaps/basemaps` | `5.7.2` | npm integrity `sha512-K1Yk6bWdULulYg+R2QRVXx4NzJZan5YQhpejEG0c1/sXruJrfPIPZuakpf3jwAgVmjIRVQwAv+yRafDeN0aaUQ==`; tarball SHA-256 `2d5d41b29cdd2364f7092ad439bd9d170b4f38cbec8858cefbe84d0f98125ce6` | gitHead `3ea8293a28131c3dc63f1bb20827bdb8a76df06f` |
| `esbuild` | `0.28.2` | npm integrity `sha512-HKVLS8dvII+xoKW9kmqxbRKrnWEXfJJr/FZhhJmiqIB0e053QNYFqOBouTMO/k5sID4MvCiUCvv8b9M4h32wIA==`; tarball SHA-256 `e045f94c235c7adc50e77ba2a579c7bec41b496b6b47c3a7845f7c1e19959a88` | tag `v0.28.2`, commit `609683d892977362a0f99026cb74b96263d728a9` |

In a temporary directory outside the repository, the exact command was:

```text
.tools/jsdeps/node_modules/.bin/esbuild src/vector_entry.js --bundle --format=iife --outfile=vendor/trip-vector-cli.js
```

The output was 72.2kb, and the generated bytes matched both the historical and candidate tracked bundles (`cmp` exit 0). Generated SHA-256 was exactly `fccb368f5ec3662547599238ca893761fa17f52ca25eb1875ad89db1ef51554d`. A minified control produced SHA-256 `de33478707ee64eae78522a222264c7c5f52fcf799d0ba5efca75bad1b0882b7` and did not match, confirming that unminified IIFE options are part of the recovered build identity.

## Playwright boundary

The candidate pins `playwright==1.62.0` with PyPI wheel hash `db755ab27db21a04186f1fe8169888e42356086e439b1059b923ef417f0b6034`. The exact package-defined browser mapping is:

- Chromium revision `1234`, browser `151.0.7922.34`; macOS ARM archive path `builds/cft/151.0.7922.34/mac-arm64/chrome-mac-arm64.zip`.
- Chromium headless shell revision `1234`, same browser version; archive path `builds/cft/151.0.7922.34/mac-arm64/chrome-headless-shell-mac-arm64.zip`.
- Firefox revision `1538`, browser `153.0`; archive path `builds/firefox/1538/firefox-mac-arm64.zip`.
- WebKit revision `2336`, browser `26.5`; macOS 14/ARM override revision `2251`, archive path `builds/webkit/2251/webkit-mac-14-arm64.zip`.
- FFmpeg revision `1011`.

The exact `python -m playwright install --list` 1.62.0 block reported `chromium-1234`, `chromium_headless_shell-1234`, `ffmpeg-1011`, `firefox-1538`, and `webkit-2336`. The package metadata inspected defines revisions, versions, paths, and mirrors but does not expose browser archive SHA/integrity values; no archive hash is claimed.

## Current advisory review

The isolated exact JS workspace returned `npm audit --json` with 64 dependencies and zero informational, low, moderate, high, or critical vulnerabilities.

- `fflate 0.8.3`: `CVE-2026-45820 / GHSA-px8p-9vwx-vf98`, affected `<0.8.3`, patched at `0.8.3`; exact embedded version passes.
- `pmtiles 4.5.0`: no maintainer advisory found and no npm-audit hit in reviewed sources; this is not treated as a universal clean bill.
- `@protomaps/basemaps 5.7.2`: no maintainer advisory found and no npm-audit hit in reviewed sources; same limitation.
- `esbuild 0.28.2`: reviewed `GHSA-gv7w-rqvm-qjhr`, `GHSA-g7r4-m6w7-qqqr`, and `GHSA-67mh-4wv8-2f99`; exact version is outside the affected ranges, and esbuild is build-time only.
- `playwright 1.62.0`: `CVE-2025-59288 / GHSA-7mvr-c777-76hp` affects `<1.55.1`; exact version passes. Playwright and Playwright-Python maintainer advisory pages had no published advisories at review time.
- Firefox 153.0/revision 1538 has Mozilla `CVE-2026-74990` advisory coverage. This is a QA-only browser cache, not part of the shipped trip-vector runtime; it is recorded and does not trigger shipped-runtime `FIX_REQUIRED`. If organizational policy treats QA browsers as deployed runtime, that policy boundary requires a separate decision.

No material known vulnerability was found in the shipped runtime bundle at the exactly reproduced versions. Chromium and WebKit were not assigned unsupported “clean” claims where an exact authoritative advisory match was not available.

## Final disposition

The trip-vector bundle’s exact package versions, registry identities, source identities, bundler/options, and reproduction hash are now evidenced. The pinned Playwright package and its package-defined browser revisions/build identifiers are also evidenced. The only explicit limitation is that Playwright does not publish archive integrity values in the inspected package metadata.

`GO_READY_EVIDENCE` is therefore returned for Project OS audit, with `verify_required` empty in `evidence.json`. This classification is intentionally narrower than any release or overall-readiness decision.
